// Test la levée d'exception lorsque le type de la condition
// de l'opérateur ternaire n'est pas booléen
int min (int a int b){
    return (a?a : b);
}

test {
    rat a = [2/3];
    rat y = (((denom a) = 3) ? [1/3] : [0/3]);
    print ((call min ((num y) 1)=1) ? true : 0);
}